{
  "name": "tax-form-annotation-spec",
  "version": "1.0.0",
  "private": true,
  "description": "A data structure and reference resolver for annotating boxes on U.S. tax forms.",
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
